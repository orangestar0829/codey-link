# 版本兼容性

本文件记录 Codey ↔ Paseo Bridge 在特定版本组合上的验证结果。**“已验证”表示列出的功能在该组合上通过检查，不表示所有更高版本或所有功能均兼容。**

[返回 README](README.md)

## 状态定义

| 状态 | 含义 |
| --- | --- |
| 已验证 | 在明确的版本组合、项目代码基线及功能范围内通过测试 |
| 未验证 | 尚无充分测试记录，不等同于不兼容 |
| 部分兼容 | 基础连接可用，但已确认某些功能失效；必须列出受影响功能 |
| 不兼容 | 已确认基础连接或核心工作流程无法完成；必须说明失败条件 |

状态按功能记录。不要因 Fast 开关测试通过就将手机显示、权限同步或上游计费一并标为已验证；“未实现”的功能也不应描述成等待测试的已有能力。

## 当前验证记录

验证日期：**2026-09-10**。项目基线：**`0.0.1`**，对应 Git 标签 `0.0.1`。

| 项目 | 验证环境 |
| --- | --- |
| Codey | `v0.10.8`；由使用者确认，补记到本次验证组合 |
| Paseo 桌面程序 / daemon | `0.7.2` |
| Paseo 手机 App | 连接日志记录为 `0.7.2`；手机验证范围见下表 |
| Codex CLI 后端 | `0.153.4` |
| 桌面应用名称 | ChatGPT（Powered by Codex & OWL）；本文称其 Codex 界面为 Codex Desktop |
| ChatGPT 桌面应用版本 | `26.903.61454`；由使用者确认，补记到本次验证组合 |
| 操作系统 | Windows |
| 启动器使用的 Node.js | `24.x`；启动器的最低版本检查为 22 |

对外兼容记录使用 ChatGPT“关于”界面显示的应用版本。即使 Codey 版本不变，ChatGPT 或 Codex CLI 后端升级后也需要分别核对。代码内部仍检查已验证的界面构建和状态结构；这些是实现细节，不作为用户需要填写的版本字段。

### 功能矩阵

| 功能 | 状态 | 已验证范围与边界 |
| --- | --- | --- |
| 复用现有 Codey 后端 | 已验证 | 请求经过已有 Desktop 连接；验证中原后端进程身份保持不变 |
| FastCtx 与具名子代理 | 已验证 | 完成真实文件读取 / 搜索和子代理调用 |
| 专用 daemon 启停 | 已验证 | 启动、复用已有 daemon、停止后保留原后端及另一套服务 |
| 已导入会话恢复 | 已验证 | 历史读取、恢复后的事件接收及完成状态；不代表所有历史格式都覆盖 |
| 模型与推理强度传递 | 已验证 | Desktop 后端到 Paseo，以及 Paseo 下一轮请求到原后端；未发送选择保持 |
| Astra Fast 开关可用 | 已验证 | 依据后端模型目录补齐开关，未知模型不会被统一放行 |
| Fast 状态双向同步 | 已验证 | Desktop 原生选择状态与 Paseo 设置接口两个方向的开启、关闭 |
| 手机 relay 接入 | 已验证 | 手机扫码连接专用服务并发送消息，以及原生客户端加密 relay 连接 |
| 设置修复后的完整手机显示流程 | 未验证 | 不把后端或设置接口检查替代为手机实际显示验收 |
| 上游实际 Fast 加速与计费 | 未验证 | 界面开关及请求选择不构成上游结果证明 |
| 长时间运行与复杂断线恢复 | 未验证 | 短时集成验证不构成长期可靠性结论 |

权限选择器的完整双向同步尚未实现，不属于本次兼容承诺。

原始日志、真实会话标识、服务身份和安装路径保留在被 Git 忽略的本地取证目录中。本文件只保存脱敏结论与公开的版本标识；自动策略测试不能独立证明上述所有集成结果。

## 升级后的验证方式

每次验证记录以下四项，不只记录 Codey 和 Paseo 两个应用版本：

1. Codey 版本。
2. ChatGPT 桌面应用“关于”界面显示的版本。
3. 实际使用的 Codex CLI 后端版本。
4. Paseo 桌面程序 / daemon 版本；测试手机时同时记录手机 App 版本。

同时记录操作系统、Node 运行时、项目自身的 tag 或 commit，以及验证日期。运行时仍需满足本机 CDP、Codey 能力注入和 Paseo 依赖条件；相同版本号不能替代这些条件检查。

建议先完成独立策略测试和后端检查：

```powershell
node tests/settings-policy-check.mjs
node tools/launch_paseo_codey.mjs --check-only
```

这两个命令通过后，再用独立测试会话验证：

- 启动专用 daemon，确认使用已有后端，基础消息与工具调用正常。
- 导入或恢复已有会话，确认输出和完成事件到达 Paseo。
- 在两个方向验证模型和推理强度，确认未发送选择不会被旧设置覆盖。
- 在两个方向验证 Fast 开启和关闭，区分原生界面状态、请求选择及上游结果；测试后恢复原设置。
- 若要声明手机兼容，单独验证手机配对、发送、重新进入会话和设置显示。
- 停止专用 daemon，确认原后端保持运行。

版本变化后先记为“未验证”。出现失败时，保存脱敏的失败条件并按功能更新状态；不要删除版本校验以代替适配，也不要把缺少测试解释为已确认不兼容。

当前原生 Fast 适配会核对 Desktop 资源和状态结构，Paseo 加载钩子会核对目标模块结构。这些检查提供明确的适配边界，但不是全面的自动兼容认证。

## 项目版本与 Release 说明

本项目独立编号，不跟随 Codey 或 Paseo 的版本号。当前项目版本及验证基线为 `0.0.1`，使用同名 Git 标签固定该版本。后续版本记录对应的实际 tag 或 commit，标签与兼容性记录一起维护。

每个 Release 至少说明：

- 本项目的 tag / commit。
- 本次验证的完整组件组合及日期。
- 哪些功能已验证，哪些仍未验证或已确认失效。
- 为适应哪个组件变化做了什么修复，是否需要重启专用 daemon 或重新检查配置。

修复新版兼容问题时追加验证记录，保留历史组合及其对应代码基线。新版本通过某个功能的检查，不会自动扩大其他功能或旧项目版本的兼容范围。

## 历史记录

| 日期 | 项目基线 | Codey | Paseo daemon | Codex CLI | ChatGPT 桌面应用 | 结论 |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-09-10 | `0.0.1` | `v0.10.8` | `0.7.2` | `0.153.4` | `26.903.61454` | 已验证上方功能矩阵中列出的项目；其他范围保持未验证 |

本表当前只有一个已记录组合。未列出的版本组合统一视为未验证，不推断向前或向后兼容。
