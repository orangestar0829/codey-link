# 版本兼容性

本文件记录 Codey ↔ Paseo Bridge 在特定版本组合上的验证结果。**“已验证”表示列出的功能在该组合上通过检查，不表示所有更高版本或所有功能均兼容。**

[返回 README](README.md)

## 状态定义

| 状态 | 含义 |
| --- | --- |
| 已验证 | 在明确的版本组合、项目代码基线及功能范围内通过测试 |
| 未验证 | 尚无充分测试记录，不等同于不兼容 |
| 部分兼容 | 基础连接可用，但已确认某些功能失效；必须列出受影响功能 |
| 不兼容 | 已确认基础连接或核心工作流程无法完成；必须说明失败条件 |

状态按功能记录。不要因 Fast 开关测试通过就将手机显示、权限同步或上游计费一并标为已验证；“未实现”的功能也不应描述成等待测试的已有能力。

## 运行条件与验证边界

验证环境为 Windows，启动器使用 Node.js `24.x`；最低版本检查为 22。Paseo 桌面程序、daemon 和手机 App 分别记录验收范围，不能用后端接口测试替代手机显示验收。

对外兼容记录使用 ChatGPT“关于”界面显示的应用版本，Codex CLI 后端版本单独记录。代码内部的界面构建和状态结构检查是适配边界，不作为用户需要填写的版本字段。

权限选择器的完整双向同步尚未实现。上游实际 Fast 加速 / 计费、长时间运行和复杂断线恢复未验证。历史表中的结论仅覆盖明确列出的功能，不推断其他组件组合或全部手机显示流程已通过。

原始日志、真实会话标识、服务身份和安装路径保留在被 Git 忽略的本地取证目录中，本文件只保存脱敏结论。

## 升级后的验证方式

每次验证记录以下四项，不只记录 Codey 和 Paseo 两个应用版本：

1. Codey 版本。
2. ChatGPT 桌面应用“关于”界面显示的版本。
3. 实际使用的 Codex CLI 后端版本。
4. Paseo 桌面程序 / daemon 版本；测试手机时同时记录手机 App 版本。

同时记录操作系统、Node 运行时、项目自身的 tag 或 commit，以及验证日期。运行时仍需满足本机 CDP、Codey 能力注入和 Paseo 依赖条件；相同版本号不能替代这些条件检查。

建议先完成独立策略测试和后端检查：

```powershell
node tests/settings-policy-check.mjs
node tools/launch_paseo_codey.mjs --check-only
```

这两个命令通过后，再用独立测试会话验证：

- 启动专用 daemon，确认使用已有后端，基础消息与工具调用正常。
- 导入或恢复已有会话，确认输出和完成事件到达 Paseo。
- 在两个方向验证模型和推理强度，确认未发送选择不会被旧设置覆盖。
- 在两个方向验证 Fast 开启和关闭，区分原生界面状态、请求选择及上游结果；测试后恢复原设置。
- 若要声明手机兼容，单独验证手机配对、发送、重新进入会话和设置显示。
- 停止专用 daemon，确认原后端保持运行。

版本变化后先记为“未验证”。出现失败时，保存脱敏的失败条件并按功能更新状态；不要删除版本校验以代替适配，也不要把缺少测试解释为已确认不兼容。

当前原生 Fast 适配会核对 Desktop 资源和状态结构，Paseo 加载钩子会核对目标模块结构。这些检查提供明确的适配边界，但不是全面的自动兼容认证。

## 项目版本与 Release 说明

本项目独立编号，不跟随 Codey 或 Paseo 的版本号。最新已发布源码标签为 `0.0.1`；后续修复在历史表中先使用对应代码 commit 作为基线。

升级过程、问题定位和逐项测试结果写入 Git 提交说明，兼容文档只维护最终版本组合与结论。历史表的日期采用记录落入仓库的提交日期，格式为 `YYYY-MM-DD`，不写测试时间范围。

发布时先把本次发布记录的项目基线更新为拟发布 tag（例如 `0.0.2`），同步 README 中的项目版本并提交，再在该提交上创建同名 tag。保留旧版本行，不移动旧 tag。

Release 从目标 tag 内的历史表选取项目基线与 tag 一致的记录，并附带运行条件与验证边界；没有匹配记录则停止打包。其他 commit 的验证记录不会混入该 Release。旧标签的原始文档仍可由打包脚本兼容读取。

## 历史记录

| 日期 | 项目基线 | Codey | Paseo daemon | Codex CLI | ChatGPT 桌面应用 | 结论 |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-09-10 | `0.0.1` | `v0.10.8` | `0.7.2` | `0.153.4` | `26.903.61454` | 基础桥接、真实 FastCtx / 子代理调用、daemon 启停、导入历史恢复及模型 / 强度 / Fast 双向传递通过；手机扫码和发消息通过，完整手机显示流程未验证 |
| 2026-09-10 | `dc8def2` | `v0.10.8` | `0.7.2` | `0.153.4` | `26.903.71938` | 修复新版 Fast 适配与故障隔离；真实消息、FastCtx、重连历史、模型 / 强度 / Fast 双向同步通过；用户反馈手机 Fast 同步通过，其他手机流程未验证 |
| 2026-09-11 | `dc8def2` | `0.10.9` | `0.7.2` | `0.153.4` | `26.903.71938` | 真实消息、FastCtx、重连历史、模型 / 强度双向传递、未发送选择保护及 Fast 双向开关通过；策略和故障隔离回归通过，同步无新增错误；手机未重复测试 |

未列出的版本组合统一视为未验证，不推断向前或向后兼容。
